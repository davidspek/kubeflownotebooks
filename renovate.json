{
  "extends": [
    "config:base",
    ":dependencyDashboard",
    ":prHourlyLimitNone"
  ],
  "schedule": [
    "before 11pm"
  ],
  "packageRules": [
    {
      "matchDatasources": [
        "docker"
      ],
      "matchUpdateTypes": [
        "major"
      ],
      "enabled": true
    },
    {
      "matchFiles": ["requirements-cpu.txt", "requirements-cuda.txt"],
      "manager": "python"
    },
    {
      "description": "Group Conda and Miniforge",
      "matchPackagePatterns": ["^conda[^\\s]+?[a-z-].*"],
      "groupName": "conda",
      "additionalBranchPrefix": ""
    }
  ],
  "regexManagers": [
    {
      "fileMatch": [
        "(^|/|\\.)Dockerfile$",
        "(^|/)Dockerfile\\.[^/]*$"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+?) depName=(?<depName>[^\\s]+?)(?: lookupName=(?<lookupName>[^\\s]+?))?(?: versioning=(?<versioning>[a-z-]+?))?\\s(?:ENV|ARG) .+?_VERSION=(?<currentValue>.*)?"
      ]
    }
  ]
}
